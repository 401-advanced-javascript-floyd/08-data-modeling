'use strict';
//products
// _id, category, name, display_name, description
const uuid = require('uuid/v4');

const schema = {
};

class Products {

  constructor() {
    this.database = [];
  }

  get(id) {
  }
  
  post(entry) {
  }

  put(id, entry) {
  }

  delete(id) {
  }

  sanitize(entry) {
  }

}

module.exports = Products;
